# Background

Drop your background image here, e.g. `meadow.png`.

- Format: PNG (or JPG if it has no transparent areas)
- One flat painted scene, not tiled
- Roughly 2000x1400px or larger works well with the current camera setup
